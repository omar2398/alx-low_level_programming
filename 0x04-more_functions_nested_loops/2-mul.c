#include <stdio.h>
#include "main.h"

/**
 * mul - is an internal function
 * Description: 'the program's description'
 * @a : input 1
 * @b : input 2
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
