#include <stdio.h>

/**
 * _strlen - Entry point
 *
 * Description: 'the program's description'
 * @s : input parameters
 *
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
	return (strlen(s));
}
